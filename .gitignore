node_modules
.next
out
.env
.env.local
.env*.local
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
next-env.d.ts

# Cursor / local agent settings
.claude/
.cursor/

# project-specific
.data
# Legacy gallery images (public/uploads/galerie/*) live in the repo since
# they were scraped from the old Joomla site; new user uploads go via S3/MinIO.
public/uploads/tmp
/tmp
